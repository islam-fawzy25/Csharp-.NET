@model IEnumerable<DataBaseFirstDemo.Models.Product>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pUnitPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pUnitPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pId }) |
            @Html.ActionLink("Details", "Details", new { id=item.pId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pId })
        </td>
    </tr>
}

</table>
